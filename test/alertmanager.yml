global:
  resolve_timeout: 5m

route:
  receiver: webhook
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1m

receivers:
  - name: webhook
    webhook_configs:
      - url: http://host.docker.internal:9999/alerts
        send_resolved: true
